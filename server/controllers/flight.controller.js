const FlightModel = require("../models/flight");
Model = require("../models/flight");
require("dotenv").config();
const secret = process.env.SECRET;

exports.createFlight = async (res,req) => {
    if(!user) {
        
    }
}
